
##checkout:
##  post:
##    - git submodule sync
##    - git submodule update --init
    
##dependencies:
##  override:
##    - cd jersey
##    - mvn clean install -Dskip test

## Custom notifications
notify:
  webhooks:
    # A list of hashes representing hooks. Only the url field is supported.
    - url: http://36.45.174.74:9090/plugins/hiworkis/api/webhook/adaa02db-fd3f-49e0-8db8-f155d143d96a
