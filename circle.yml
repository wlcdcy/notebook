

##checkout:
##  post:
##    - git submodule sync
##    - git submodule update --init
    
##dependencies:
##  override:
##    - cd jersey
##    - mvn clean install -Dskip test

## Custom notifications
notify:
  webhooks:
    # A list of hashes representing hooks. Only the url field is supported.
    - url: http://36.46.42.147/jersey/webhook/circleci/123
